<div class="container">
  <h1>Tasks</h1>
  <ul>
  <% @tasks.each do |task| %>
    <li>
      <span><input type="checkbox" disabled="disabled" <%= checked="checked" if task.completed %> ><%= link_to task.title, task_path(task) %></span>
      <span><%= link_to "🗑", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %></span>
    </li>
  <% end %>
  </ul>
  <%= link_to 'Add a new task', new_task_path  %>
</div>
